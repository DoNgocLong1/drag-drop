import { Button, Modal } from 'antd';
import styled from 'styled-components';

export const CustomButton = styled(Button)`
    padding:10px 20px;
    height: 100%;
    font-size: 15px;
    font-weight: bold;
`

export const CustomModel = styled(Modal)`
    .ant-btn {
        padding:10px 20px;
        height: 100%;
        font-size: 15px;
        font-weight: bold;
    }
`

